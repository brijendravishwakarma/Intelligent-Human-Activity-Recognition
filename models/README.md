# Models

The trained model files are not included in this repository because of their large size.

Required files:

- `best_cnn_lstm_attention.pth`
- `yolo11s.pt`

Place both files inside this directory before running the application.
